engines:
  rubocop:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        ruby:
          mass_threshold: 25

ratings:
  paths:
    - lib/**/*.rb

exclude_paths:
  - spec/**/*
